import { IdleDirective } from './idle.directive';

describe('IdleDirective', () => {
  it('should create an instance', () => {
    const directive = new IdleDirective();
    expect(directive).toBeTruthy();
  });
});
